/** vitest stub for 'virtual:pwa-register' (see vitest.config.ts alias). */
export function registerSW(_options?: unknown): (reloadPage?: boolean) => Promise<void> {
  return () => Promise.resolve();
}
